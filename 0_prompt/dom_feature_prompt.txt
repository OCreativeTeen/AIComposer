任务：请根据我提供的页面代码/数据，定位并编写自动点击“宽高比为 9:16”选项的操作逻辑（如 Playwright / Selenium / Puppeteer 脚本）。

请按以下步骤帮我提取特征并生成代码：

1. 特征提取与定位（宽高比按钮）：
   - 在输入框下方一排图标按钮（Icon Buttons）中，定位用于设置“宽高比”（Aspect Ratio）的按钮。
   - 提取该按钮在 DOM / HTML 代码中的关键特征（如 class、aria-label、data-testid、xpath 或文本标识）。

2. 特征提取与定位（9:16 选项）：
   - 点击“宽高比”按钮后，会弹出选择菜单/下拉框。
   - 在所有宽高比选项中，精准定位数值为 "9:16" 的选项。
   - 【重要防错规则】：选项文本中可能包含 "9:16" 以及后面跟随的其他描述性文字或标注，请完全忽略 9:16 后面的所有文字内容，只需确保模糊匹配或匹配开头包含 "9:16" 即可，切勿使用精确全匹配。

3. 输出要求：
   - 提取并列出“宽高比按钮”与“9:16 选项”的准确 Selector/XPath/特征数据。
   - 提供一段完整的自动化操作代码，逻辑包含：
     ① 找到并点击“宽高比”按钮；
     ② 等待菜单弹窗加载；
     ③ 找到并点击包含 "9:16" 的选项。

页面代码/数据如下：
[请在这里粘贴你的 Grok Image 页面源码/DOM数据]